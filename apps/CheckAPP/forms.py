from django import forms
from apps.CheckAPP.models import BaseApp

# class YourPartialForm(forms.ModelForm):
#     class Meta:
#         model = BaseApp
#         fields = ['AppName', 'Country/Area', 'AG Status', 'Arch', 'Download Link']
#         widgets = {
#             'PackageName': forms.ReadOnlyInput(),  # 使用 ReadOnlyInput 使 PackageName 字段只读
#         }
